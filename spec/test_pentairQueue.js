const chai = require('chai');
const expect = chai.expect;
const fs = require('fs');
const sinon = require ('sinon');
const moment = require('moment');
const path = require('path');

// const pentairQueue = require (path.resolve (__dirname + '/../server/equipment/pentair/PentairQueue'));

describe ('pentairQueue', function () {
  describe ('', function () {
    it('should ', function () {

    });
  });
});